{
	"name": <%- JSON.stringify(name) %>,
	"displayName": <%- JSON.stringify(displayName) %>,
	"description": <%- JSON.stringify(description) %>,
	"version": "0.0.1",
	"engines": {
		"vscode": <%- JSON.stringify(vsCodeEngine) %>,
        "azdata": <%- JSON.stringify(azdataEngine) %>
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		<%- JSON.stringify(`onCommand:Launch Notebooks: ${name}`) %>
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": <%- JSON.stringify(`Launch Notebooks: ${name}`) %>,
                "title":  <%- JSON.stringify(`Launch Notebooks: ${name}`) %>
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "<%= pkgManager %> run compile",
		"compile": "tsc -p ./",
		"lint": "eslint src --ext ts",
		"watch": "tsc -watch -p ./",
		"pretest": "<%= pkgManager %> run compile && <%= pkgManager %> run lint",
	},
	"devDependencies": {
		"@types/vscode": <%- JSON.stringify(vsCodeEngine) %>,
		"@types/azdata": <%- JSON.stringify(azdataEngine) %>,
		"@types/glob": "^7.1.1",
		"@types/node": "^13.11.0",
		"eslint": "^6.8.0",
		"@typescript-eslint/parser": "^2.30.0",
		"@typescript-eslint/eslint-plugin": "^2.30.0",
		"glob": "^7.1.6",
		"typescript": "^3.8.3",
		"vscode-test": "^1.3.0"
	}
}
